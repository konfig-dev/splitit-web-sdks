/*
 * splitit-web-api-v3
 * Splitit's Web API
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by Konfig (https://konfigthis.com).
 * Do not edit the class manually.
 */


package com.konfigthis.splitit.client.model;

import java.util.Objects;
import java.util.Arrays;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.konfigthis.splitit.client.model.LinksModel;
import com.konfigthis.splitit.client.model.PaymentPlanOptionModel;
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import java.io.IOException;
import java.util.ArrayList;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import org.apache.commons.lang3.StringUtils;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Map.Entry;
import java.util.Set;

import com.konfigthis.splitit.client.JSON;

/**
 * InstallmentsEligibilityResponse
 */@javax.annotation.Generated(value = "Generated by https://konfigthis.com")
public class InstallmentsEligibilityResponse {
  public static final String SERIALIZED_NAME_INSTALLMENT_PROVIDER = "InstallmentProvider";
  @SerializedName(SERIALIZED_NAME_INSTALLMENT_PROVIDER)
  private String installmentProvider;

  public static final String SERIALIZED_NAME_PAYMENT_PLAN_OPTIONS = "PaymentPlanOptions";
  @SerializedName(SERIALIZED_NAME_PAYMENT_PLAN_OPTIONS)
  private List<PaymentPlanOptionModel> paymentPlanOptions = null;

  public InstallmentsEligibilityResponse() {
  }

  public InstallmentsEligibilityResponse installmentProvider(String installmentProvider) {
    
    
    
    
    this.installmentProvider = installmentProvider;
    return this;
  }

   /**
   * Get installmentProvider
   * @return installmentProvider
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(value = "")

  public String getInstallmentProvider() {
    return installmentProvider;
  }


  public void setInstallmentProvider(String installmentProvider) {
    
    
    
    this.installmentProvider = installmentProvider;
  }


  public InstallmentsEligibilityResponse paymentPlanOptions(List<PaymentPlanOptionModel> paymentPlanOptions) {
    
    
    
    
    this.paymentPlanOptions = paymentPlanOptions;
    return this;
  }

  public InstallmentsEligibilityResponse addPaymentPlanOptionsItem(PaymentPlanOptionModel paymentPlanOptionsItem) {
    if (this.paymentPlanOptions == null) {
      this.paymentPlanOptions = new ArrayList<>();
    }
    this.paymentPlanOptions.add(paymentPlanOptionsItem);
    return this;
  }

   /**
   * Get paymentPlanOptions
   * @return paymentPlanOptions
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(value = "")

  public List<PaymentPlanOptionModel> getPaymentPlanOptions() {
    return paymentPlanOptions;
  }


  public void setPaymentPlanOptions(List<PaymentPlanOptionModel> paymentPlanOptions) {
    
    
    
    this.paymentPlanOptions = paymentPlanOptions;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the InstallmentsEligibilityResponse instance itself
   */
  public InstallmentsEligibilityResponse putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    InstallmentsEligibilityResponse installmentsEligibilityResponse = (InstallmentsEligibilityResponse) o;
    return Objects.equals(this.installmentProvider, installmentsEligibilityResponse.installmentProvider) &&
        Objects.equals(this.paymentPlanOptions, installmentsEligibilityResponse.paymentPlanOptions)&&
        Objects.equals(this.additionalProperties, installmentsEligibilityResponse.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(installmentProvider, paymentPlanOptions, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class InstallmentsEligibilityResponse {\n");
    sb.append("    installmentProvider: ").append(toIndentedString(installmentProvider)).append("\n");
    sb.append("    paymentPlanOptions: ").append(toIndentedString(paymentPlanOptions)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("InstallmentProvider");
    openapiFields.add("PaymentPlanOptions");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

 /**
  * Validates the JSON Object and throws an exception if issues found
  *
  * @param jsonObj JSON Object
  * @throws IOException if the JSON Object is invalid with respect to InstallmentsEligibilityResponse
  */
  public static void validateJsonObject(JsonObject jsonObj) throws IOException {
      if (jsonObj == null) {
        if (!InstallmentsEligibilityResponse.openapiRequiredFields.isEmpty()) { // has required fields but JSON object is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in InstallmentsEligibilityResponse is not found in the empty JSON string", InstallmentsEligibilityResponse.openapiRequiredFields.toString()));
        }
      }
      if ((jsonObj.get("InstallmentProvider") != null && !jsonObj.get("InstallmentProvider").isJsonNull()) && !jsonObj.get("InstallmentProvider").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `InstallmentProvider` to be a primitive type in the JSON string but got `%s`", jsonObj.get("InstallmentProvider").toString()));
      }
      if (jsonObj.get("PaymentPlanOptions") != null && !jsonObj.get("PaymentPlanOptions").isJsonNull()) {
        JsonArray jsonArraypaymentPlanOptions = jsonObj.getAsJsonArray("PaymentPlanOptions");
        if (jsonArraypaymentPlanOptions != null) {
          // ensure the json data is an array
          if (!jsonObj.get("PaymentPlanOptions").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `PaymentPlanOptions` to be an array in the JSON string but got `%s`", jsonObj.get("PaymentPlanOptions").toString()));
          }

          // validate the optional field `PaymentPlanOptions` (array)
          for (int i = 0; i < jsonArraypaymentPlanOptions.size(); i++) {
            PaymentPlanOptionModel.validateJsonObject(jsonArraypaymentPlanOptions.get(i).getAsJsonObject());
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!InstallmentsEligibilityResponse.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'InstallmentsEligibilityResponse' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<InstallmentsEligibilityResponse> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(InstallmentsEligibilityResponse.class));

       return (TypeAdapter<T>) new TypeAdapter<InstallmentsEligibilityResponse>() {
           @Override
           public void write(JsonWriter out, InstallmentsEligibilityResponse value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additonal properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   obj.add(entry.getKey(), gson.toJsonTree(entry.getValue()).getAsJsonObject());
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public InstallmentsEligibilityResponse read(JsonReader in) throws IOException {
             JsonObject jsonObj = elementAdapter.read(in).getAsJsonObject();
             validateJsonObject(jsonObj);
             // store additional fields in the deserialized instance
             InstallmentsEligibilityResponse instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of InstallmentsEligibilityResponse given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of InstallmentsEligibilityResponse
  * @throws IOException if the JSON string is invalid with respect to InstallmentsEligibilityResponse
  */
  public static InstallmentsEligibilityResponse fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, InstallmentsEligibilityResponse.class);
  }

 /**
  * Convert an instance of InstallmentsEligibilityResponse to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

